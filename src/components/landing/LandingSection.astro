---
export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<span class="font-extrabold pr-1">
	<span class="pr-1" style="color: var(--color-accent-500);">
		<slot />
	</span>
	<span style="color: var(--color-accent-700);">{title}</span>
</span>
